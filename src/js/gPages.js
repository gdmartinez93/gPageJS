'use stric'

export default class gPages
{
    constructor( elem, options )
    {
        let self = this

        $(window).resize( ()=> {

        })

        $(window).on("orientationchange", ()=> {

        })

        $( window ).scroll( ()=> {

        })
    }
}
